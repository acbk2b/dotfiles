onboarding = false

[theme]
name = "tokyo-night"
auto_switch = false

[keys]
# Match the tmux prefix
prefix = "ctrl+space"
# Swap the focused pane with its neighbour
swap_pane_left = "prefix+shift+h"
swap_pane_down = "prefix+shift+j"
swap_pane_up = "prefix+shift+k"
swap_pane_right = "prefix+shift+l"

[ui]
# tmux-style shared splitters, no outer frame (hyprland already adds gaps)
pane_gaps = false
pane_outer_borders = false

[ui.toast]
delivery = "system"

# Clear the focused pane (ctrl+l is taken by neovim pane navigation)
[[keys.command]]
key = "ctrl+q"
type = "shell"
command = "herdr pane send-keys \"$HERDR_ACTIVE_PANE_ID\" ctrl+l"
